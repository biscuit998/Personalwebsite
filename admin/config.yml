backend:
  name: github
  repo: biscuit998/Personalwebsite
  branch: main
  base_url: https://personalwebsitebackend.bliss9988420.workers.dev
  auth_endpoint: /auth
media_folder: "static/uploads"
public_folder: "/static/uploads"

collections:
  - name: site
    label: 站點設定
    editor: { preview: false }
    files:
      - file: "data/site.json"
        label: 基本資訊
        name: basics
        fields:
          - { label: 公司名稱, name: company, widget: string }
          - { label: 標語, name: tagline, widget: string, required: false }
          - { label: 首屏標題, name: hero_title, widget: text }
          - { label: 首屏說明, name: hero_desc, widget: text }
          - { label: 電話, name: phone, widget: string }
          - { label: 電郵, name: email, widget: string }
          - { label: WhatsApp, name: whatsapp, widget: string, required: false }
          - { label: 封面圖片, name: cover, widget: image, required: false }
  # 之後我可以幫你再加：products、feedback 兩個集合

